const GENDER = {
  MALE: 'male',
  FEMALE: 'female',
  OTHER: 'other',
};

const USER_ROLE = {
  USER: 'user',
  ADMIN: 'admin',
};

const SALT_WORK_FACTOR = 10;

export { GENDER, USER_ROLE, SALT_WORK_FACTOR };
